// Mock API configuration - no backend required
export const API_CONFIG = {
  BASE_URL: 'mock://localhost',
  WS_URL: 'mock://localhost/ws',
};

console.log('Mock API Config:', API_CONFIG);

export default API_CONFIG;
